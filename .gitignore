# Arquivos temporários do Python (não precisam ir pro servidor)
__pycache__/
*.pyc
*.pyo

# Ambiente virtual
venv/
.venv/

# Arquivos auxiliares de banco SQLite (NÃO ignorar o usuarios.db!)
*.db-journal

# NUNCA subir a chave da Asaas pro GitHub (arquivo temporário de conferência)
chave_producao.txt
